# บริบทโปรเจกต์ (สำหรับ Claude Code)

เนื้อหาทั้งหมดอยู่ในไฟล์ `AGENTS.md` ซึ่งเป็นมาตรฐานกลางที่ AI agent ตัวอื่นอ่านได้ด้วย
ไฟล์นี้มีไว้เพื่อให้ Claude Code ดึงเนื้อหานั้นเข้ามาโดยอัตโนมัติ

@AGENTS.md
